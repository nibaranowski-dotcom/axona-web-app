The Axona email-capture row — input fused to a lime button, used in the hero and closing CTA. Omit `buttonLabel` for a plain bordered input.

```jsx
<Input buttonLabel="Book a demo" onSubmit={(email) => track(email)} />
```

Props: `placeholder`, `buttonLabel`, `onSubmit(value)`.
