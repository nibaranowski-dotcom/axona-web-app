Monospace key→value chip for "agents at work" stat strips, hero counters, and in-product data captions.

```jsx
<StatChip label="POs drafted" value="13,422" />
<StatChip value="7,491,284 parts" />
```

Props: `label` (optional mono caption), `value`.
