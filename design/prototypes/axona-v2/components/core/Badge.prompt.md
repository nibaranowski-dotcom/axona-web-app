Tiny label badge. `accent` for "Coming soon" / highlights, `success` for live/approved/auto-applied states, `neutral` for quiet metadata.

```jsx
<Badge tone="accent">Coming soon</Badge>
<Badge tone="success">Auto-applied</Badge>
```

Tones: `accent` (lime) · `success` (green tint) · `neutral` (grey).
