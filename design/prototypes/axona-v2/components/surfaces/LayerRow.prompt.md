Full-width "layer" row for the architecture stack (L1–L4) — a mono tag on the left, title + body on the right, on the warm-grey surface.

```jsx
<LayerRow tag="L1 · DATA & CONTEXT" title="Data & Context"
  body="Connectors to ERP, PLM, MES, telemetry, and docs — plus an ontology of how your company works." />
```

Stack several vertically with a small gap to render the platform architecture.
